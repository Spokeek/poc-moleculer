version: '3'
services:
  api:
    build:
      context: app/microservices
      dockerfile: ./Dockerfile
      args:
        MICROSERVICE: api
    ports:
      - '4000:3000'
  authentification:
    build:
      context: ./app/microservices
      dockerfile: ./Dockerfile
      args:
        MICROSERVICE: authentification
  hello:
    build:
      context: ./app/microservices
      dockerfile: ./Dockerfile
      args:
        MICROSERVICE: hello
